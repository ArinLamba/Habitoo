import { Text } from "react-native";

import { Screen } from "../../../shared/ui/screen";

export function NotesScreen() {
  return (
    <Screen>
      <Text className="text-base font-semibold text-white">Notes</Text>
    </Screen>
  );
}
